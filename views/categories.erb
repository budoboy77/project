<div class="well">
  <table class="table">
    <thead>
      <tr>
        <th>Category Name</th>
      </tr>
    </thead>
    <tbody>
      <% if params[:id] != "new" %>
       <tr>
        <td><%== text_field_tag :name, Category.find(params[:id]).name, size: 40 %></td>
      </tr>
  </table>
  <%== submit_tag "Update Category" %>
      <% else %>
      <tr>
        <td><%== text_field_tag :name, "", size: 40 %></td>
      </tr>
  </table>
  <%== submit_tag "Add Category" %>
  <% end %>
</tbody>
</div>
<div class="well">
  <table class="table table-striped table-hover table-bordered table-fixed">
  <thead>
    <tr>
      <th></th>
      <th>Name</th>
    </tr>
  </thead>
  <tbody>
    <% @categories.each do |category| %>
    <tr>
      <td><a href="/admin/categories/<%= category.id %>">Edit</a> <%== submit_tag "delete" %></td>
      <td><%= category.name %></td>
    </tr>
    <% end %>
  </tbody>
</table>
</div>